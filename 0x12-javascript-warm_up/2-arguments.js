#!/usr/bin/node
const args = process.argv.slice(2);
if (args.length === 0) console.log('No argument');
if (argv.length === 1) console.log('Argument found');
if (argv.length > 1) console.log('Arguments found');
